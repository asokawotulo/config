# Firecrawl API key used by the firecrawl extension.
FIRECRAWL_API_KEY=fc-your-api-key

# Optional: override the default shared cache directory (~/.firecrawl/results).
# FIRECRAWL_CACHE_DIR=/absolute/path/to/firecrawl/results
