---
description: Investigate a coding task and produce an implementation-ready plan
argument-hint: "<task>"
---
${ARGUMENTS:-Use the current conversation to create an implementation ready plan.}

Before responding, load the skill named `planner` and follow it exactly.
