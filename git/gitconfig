[user]
	name = Asoka Wotulo
	email = asokawotulo@yahoo.com

[core]
	excludesfile = ~/.gitignore
	editor = code --wait

[diff]
	tool = code

[difftool]
	code = code --wait --diff

[merge]
	tool = code

[mergetool]
	code = code --wait

[pull]
	rebase = true